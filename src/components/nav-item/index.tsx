import { NavItemIcon } from "./icon";
import { NavItemLabel } from "./label";
import { NavItemRoot } from "./root";

export const NavItem = {
  Root: NavItemRoot,
  Icon: NavItemIcon,
  Label: NavItemLabel,
};
